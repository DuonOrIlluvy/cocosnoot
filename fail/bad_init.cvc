// Should produce a type checking error 0 is an int literal, not a float.

#include "civic.h"

export int main() {
    // error: Wrong assignment type, expected float but got int
    float a = 0;
    return 0;
}
