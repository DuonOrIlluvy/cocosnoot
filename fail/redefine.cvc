// Should produce a context analysis error because of a redefined variable.

#include "civic.h"

export int main() {
    int a = 0;
    // error: Variable 'a' is already declared
    float a = 5.0;
    return 0;
}
