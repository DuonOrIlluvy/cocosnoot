// Should produce a type checking error because funcall has bad arguments.

#include "civic.h"

int kaas(int a) {
    return 0;
}

export int main() {
    int a = 0;
    // error: Left and right operand of binary operator have non matching types,
    // left operand has type int and right operand has type float
    kaas(5 * 6.0);
    return 0;
}
