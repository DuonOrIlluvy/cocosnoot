// Modulus only works on integers.

#include "civic.h"

export int main() {
    // error: only int operands are allowed for modulus, left operand has type
    // float and right operand has type float
    float mod = 6.0 % 5.0;
    return 0;
}
