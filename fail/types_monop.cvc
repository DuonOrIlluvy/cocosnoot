// Should produce a type checking error because monops also have a type.

#include "civic.h"

export int main() {
    int a = 0;
    // error: Wrong assignment type, expected int but got float
    a = -5.0;
    return 0;
}
